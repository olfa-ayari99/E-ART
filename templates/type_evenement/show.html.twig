<!DOCTYPE html>

<title>TypeEvenement</title>

{% block body %}
    <h1>TypeEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_evenement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_evenement.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_evenement_index') }}">back to list</a>

    <a href="{{ path('app_type_evenement_edit', {'id': type_evenement.id}) }}">edit</a>

    {{ include('type_evenement/_delete_form.html.twig') }}
{% endblock %}
